// Achievement metadata is owned by @claude-cope/shared so the backend can
// validate IDs against the same source of truth (preventing client/server drift).
export { ALL_ACHIEVEMENTS } from "@claude-cope/shared/achievements";
